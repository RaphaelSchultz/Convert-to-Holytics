# 📦 Conteúdo do ZIP para PythonAnywhere

## ✅ Arquivos Incluídos:

```
convert-to-holytics-deploy.zip
├── app/                    # Código da aplicação
│   ├── __init__.py
│   ├── config.py
│   ├── routes/
│   ├── services/
│   ├── repositories/
│   └── utils/
├── static/                 # CSS e JavaScript
│   ├── css/
│   └── js/
├── templates/              # HTML
│   └── index.html
├── run.py                  # Entry point
└── requirements.txt        # Dependências Python
```

## 📝 Próximos Passos:

1. ✅ **ZIP criado!** → `convert-to-holytics-deploy.zip`

2. **No PythonAnywhere:**
   - Upload do ZIP
   - Extraia na pasta home
   - Siga o guia `DEPLOY_PYTHONANYWHERE.md`

## 🔍 Arquivos Excluídos:

❌ `.git/` (controle de versão)  
❌ `.gitignore`  
❌ `README.md` (documentação)  
❌ `DEPLOY_*.md` (guias)  
❌ `__pycache__/` (cache Python)  
❌ `.env` (configurações locais)  
❌ Arquivos de build/dist  

**Só o essencial para rodar!** 🚀
